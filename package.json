{
  "name": "cypress-pageobject-allure",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:allSpecs": "cypress run --reporter mocha-allure-reporter",
    "cy:spec": "cypress run --spec cypress/e2e/login.cy.js --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report",
    "testAll:allure": "npm run cy:allSpecs && npm run report:allure",
    "testOne:allure": "npm run cy:spec && npm run report:allure"
  },
  "author": "Hamza Qureshi",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.5.1",
    "prettier": "^2.8.4"
  },
  "devDependencies": {
    "allure-commandline": "^2.20.1",
    "mocha-allure-reporter": "^1.4.0"
  }
}
